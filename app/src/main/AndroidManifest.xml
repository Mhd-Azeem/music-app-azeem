<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".WavelengthApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_classic"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_classic"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wavelength"
        android:usesCleartextTraffic="false">

        <!-- MainActivity itself is not the launcher entry point: the activity-aliases below are,
             so that switching the "app icon" (Settings > App icon) is just enabling one alias
             and disabling the others (android:icon can't be changed to an arbitrary image at
             runtime — Android requires it to be a build-time resource). -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Wavelength" />

        <activity-alias
            android:name=".IconClassic"
            android:enabled="true"
            android:exported="true"
            android:icon="@drawable/ic_launcher_classic"
            android:roundIcon="@drawable/ic_launcher_classic"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".IconPhoto1"
            android:enabled="false"
            android:exported="true"
            android:icon="@drawable/ic_launcher_photo"
            android:roundIcon="@drawable/ic_launcher_photo"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <service
            android:name=".playback.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

    </application>

</manifest>
